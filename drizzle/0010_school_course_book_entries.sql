CREATE TABLE IF NOT EXISTS school_course_book_entries (
 id TEXT PRIMARY KEY NOT NULL, upload_id TEXT NOT NULL REFERENCES reference_uploads(id), academic_year INTEGER NOT NULL, entry_year INTEGER NOT NULL, current_grade INTEGER, target_grade INTEGER, target_semester INTEGER, subject_group TEXT NOT NULL DEFAULT '', subject_name TEXT NOT NULL, curriculum_category TEXT NOT NULL DEFAULT '', school_requirement TEXT NOT NULL DEFAULT 'optional', selection_group TEXT NOT NULL DEFAULT '', selection_count TEXT NOT NULL DEFAULT '', credit TEXT NOT NULL DEFAULT '', timing TEXT NOT NULL DEFAULT '', selection_conditions TEXT NOT NULL DEFAULT '', concurrent_limit TEXT NOT NULL DEFAULT '', prerequisite TEXT NOT NULL DEFAULT '', subsequent TEXT NOT NULL DEFAULT '', csat_priority INTEGER NOT NULL DEFAULT 0, humanities_priority INTEGER NOT NULL DEFAULT 0, science_priority INTEGER NOT NULL DEFAULT 0, source_location TEXT NOT NULL DEFAULT '', admin_note TEXT NOT NULL DEFAULT '', created_at TEXT NOT NULL, updated_at TEXT NOT NULL
);
--> statement-breakpoint
CREATE INDEX IF NOT EXISTS idx_school_course_book_entries_lookup ON school_course_book_entries(entry_year,target_grade,target_semester,subject_name);
