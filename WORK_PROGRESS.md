# 현재 작업

## 작업 목표

선생님 페이지의 학생 확인 화면을 승인 중심이 아닌 예외 학생 점검·수정 요청 중심으로 개편한다.

## 완료

- 학생 프로필에 인문계열·자연계열·아직 미정 선택값을 추가했고, 계열 변경은 신청안 편집 가능 상태에서 언제든 저장할 수 있게 함.
- 학교가 직접 정하는 계열별 과목 선택 기준을 위한 `academic_track_course_guides` 구조를 추가함. 필수·1순위·2순위·3순위는 코드에 고정하지 않고 선생님이 직접 등록한다.
- 학생 화면은 선택 계열에 맞는 학교 필수 과목, 우선순위별 과목, 현재 선택 여부, 대학 핵심·권장 중복 배지, 교육청 과목 설명 보기를 함께 표시함.
- 미정 학생은 인문/자연 기준을 임의 적용하지 않고 학교 공통 기준과 희망 대학·학과 자료만 표시함.
- 선생님 기준자료 관리에 계열별 기준 등록·적용 해제 화면을 추가하고, 학생 상세에서도 동일한 계열 가이드와 충족 현황을 표시함.
- 학교 필수 과목 미선택은 명확한 경고로, 우선순위·대학 추천과목 미선택은 제출을 막지 않는 상담 참고 경고로 처리함.
- `school_course_book_entries`에 교육과정 분류, 학교 운영 필수/선택, 수능 준비 우선, 인문/자연 우선을 서로 분리해 저장함.
- HWPX는 표·문단 구조를 분석한 뒤 수동 수정·미리보기·최종 저장 순서로만 반영하며, 교육청 PDF는 과목 설명 참고자료로 별도 보존함.
- 저장 후 선생님은 과목별 기준을 수정하고 선택한 여러 과목의 수능 준비/인문/자연 우선을 일괄 지정·해제할 수 있음.
- 학생·담임 화면은 학교 운영 필수·수능 준비 우선·선택 계열 우선을 대학 핵심·권장과목과 별도로 표시하고, 미선택은 제출 차단 없는 경고로 처리함.
- 선생님 메뉴의 `학생 확인`을 `신청안 점검`으로 바꾸고, 제출 상태·확인 필요 사유·수정 요청 중심의 반별 목록으로 개편함.
- 신청안 점검은 반→번호→이름 순으로 정렬하며, 반 빠른 선택·차수·상태·계열·확인 필요/정상 필터를 복합 적용할 수 있음.
- 확인 필요는 학교 필수·수능 준비 우선·계열 우선·대학 핵심/권장 미선택, 희망대학/학과 미입력, 재제출, 과목 데이터 이상, 기존 매칭 이슈를 담임 참고용 사유로 표시함.
- 정상 학생은 `제출 완료` 또는 `특이사항 없음`으로 조용히 표시하고, 담임의 유일한 조치는 문제가 있을 때의 `수정 요청`으로 유지함.

- 새 신청안 상태를 `작성 중 → 제출 완료 → 수정 요청 → 재제출 완료`으로 추가했고, 기존 `pending/confirmed/recheck_required` 값은 과거 데이터 호환용으로 유지함.
- 제출 완료 상태에서는 학생 과목·진로 정보를 잠그고, 담임의 수정 요청 상태에서만 다시 편집할 수 있도록 변경함.
- 담임 화면에서 확인 완료 버튼을 제거하고, 문제가 없는 신청안은 별도 처리 없이 유지하며 수정 요청과 코멘트만 처리하도록 변경함.
- 마지막 제출 시점의 과목·진로 스냅샷을 `plan_submission_snapshots`에 보존해 수정 요청 뒤 편집 중인 화면 데이터와 분리함.
- 공식 결과 검증은 새 제출본·재제출본의 마지막 스냅샷을 우선 사용하고, 기존 승인형 과거 데이터는 기존 과목 데이터로 계속 비교하도록 호환 처리함.
- 대시보드와 학생 목록을 미제출·제출 완료·수정 요청·재제출 완료 및 추천과목 확인 필요 중심으로 변경함.

- 교육청 PDF의 실제 PDF.js 텍스트 구조(표 셀이 띄어쓰기·반복 조각으로 반환됨)를 재확인하고, 다중 공백·반복 셀 조각을 정리한 후 과목 상세 페이지만 판별하도록 보완함.
- 교육청 공식 과목 안내는 2022 개정 교육과정 자료라는 출처를 보존하면서, 선택한 입학년도에 연결해 학생 화면의 과목 설명으로 조회되도록 수정함.
- 기존 학교 실제 선택 가능 과목 안내 설명과 교육청 PDF 설명을 함께 조회하도록 회귀 보완했고, PDF 설명에는 관련 과목·위계, 이수 학점, 수능 관련 정보를 학생 상세 보기에서 표시하도록 연결함.

- 운영 브라우저 콘솔에서 계열별 파일 처리 중 `ReferenceError: index is not defined` React 렌더링 예외를 확인함.
- 계열별 매트릭스 변환에서 선언되지 않은 `index` 접근을 제거하고, 과목 열 인덱스와 매핑 인덱스를 올바르게 비교하도록 수정함.
- 계열별 자료는 `영역 / 세부 과목` 헤더를 분리하고 대학명이 있는 셀만 변환하며 빈칸·대시 셀을 제외하도록 보완함.
- 기준자료 업로드 영역에 오류 경계를 추가하여 분석 UI 오류가 페이지 전체 크래시로 이어지지 않도록 함.
- 계열별 매트릭스 회귀 테스트를 추가해 자동 테스트 7건 통과.

- 실제 운영 화면에서 계열별 파일이 1행만 헤더로 인식되어 `계열`·`학과` 행까지 데이터로 처리되는 원인을 확인함.
- 계열별 파일은 1~2행의 다중 헤더를 우선 선택하고, `모집단위 / 계열`과 `모집단위 / 학과`를 각각 계열·모집단위 열로 매핑하도록 수정함.
- 내부 JSON 미리보기 출력을 제거하고, 실제 저장될 계열·모집단위·과목 영역·세부 과목·반영 대학 형태의 최대 10건 미리보기로 변경함.
- 자동 제외 행(빈 행·주석·반영 대학이 없는 행)과 실제 오류 행(반영 대학은 있으나 모집단위가 없는 행)을 분리해 표시하도록 수정함.
- 괄호 안 쉼표를 보존하는 대학 목록 분리, 과목 열/과목 셀/과목-대학 관계 수를 각각 표시, 오류 또는 관계 0건이면 최종 저장 비활성화를 추가함.
- 다중 헤더 계열별 매트릭스 회귀 테스트를 추가해 `npm test` 8건, `npm run build`, `npm run lint`, `git diff --check` 통과.

- 실제 첨부 PDF의 243쪽, 전체 분류표(7~8쪽), 교과별 표지·위계, 개별 과목 상세 페이지 구조를 확인함.
- 교육청 PDF 전용 업로드, 페이지 단위 진행 표시, 개별 과목 페이지 추출, 저장 전 카드 미리보기를 구현함.
- 교육청 PDF 안내와 학교 실제 선택 가능 과목을 별도 테이블로 분리했고 PDF 저장은 학생 선택과목 목록을 바꾸지 않음.
- 자동 테스트 8건, 운영 빌드, 린트, Git 검사 통과.

- 기준자료 업로드에서 병합된 셀의 값을 실제 병합 범위 안에서만 이어받도록 보완함.
- 연속된 1~3개 헤더 행을 결합해 `반영과목 / 핵심과목`, `반영과목 / 권장과목`을 독립 열로 자동 인식하도록 구현함.
- 헤더 시작 행과 마지막 행을 직접 고를 수 있도록 하고, 열 매핑은 열 번호 기반으로 처리해 중복·복합 헤더를 안전하게 구분함.
- 필수 열 매핑 실패는 데이터 행 전체 오류 대신 파일 수준 안내로 표시하도록 보완함.
- 병합 다중 헤더 자동 인식 테스트를 추가해 자동 테스트 6건 통과.

- 전체 데이터 JSON 백업 API와 선생님 설정 화면의 다운로드 버튼을 추가함.
- 백업 파일에서 선생님 인증 상태·비밀번호·세션 secret을 제외함.
- 백업 JSON 구조 검사 및 복구 미리보기/최종 복구 단계를 분리함.
- 복구는 최종 확인 후에만 실행되며, 성공·실패 메시지와 작업 로그를 남김.
- `npm test`, `npm run build`, `npm run lint`, `git diff --check` 통과.

- 공식 결과 업로드는 파일 선택 → 분석/미리보기 → 최종 반영으로 분리됨.
- 통합 엑셀 파일을 반별 자료로 자동 분리해 저장하도록 구현됨.
- 현재 학년과 신청 대상 학년을 분리하는 D1 마이그레이션 완료.
- 학생·선생님 화면의 모바일 카드형 표시와 터치 영역을 개선함.
- 결과 검증 표의 학생 열 줄바꿈 방지 규칙 적용.
- 결과 검증 화면을 학생 그룹 구조로 변경하고 반 필터·반→번호→이름 정렬을 추가함.
- 대학별 핵심·권장과목 자료와 학교 공식 과목 안내자료의 별도 업로드 구조를 추가함.
- 기준자료는 파일 선택 → 분석/미리보기 → 최종 저장으로 분리했고, 같은 기준연도·자료 유형의 적용 자료는 확인 후 교체하도록 구현함.
- 기준자료 교체본과 적용 해제 자료는 삭제하지 않고 업로드 이력으로 보존함.
- 학교 공식 과목 안내자료의 선택 가능 과목은 학생 과목 선택용 교육과정에 반영하고, 안내 설명은 직접 입력 설명이 없을 때 학생에게 출처와 함께 표시함.
- 대학별 원문 행, 계열별 대표 모집단위 과목·대학 목록, 학교 과목 안내자료를 각각 별도 데이터 구조로 보존함.
- 업로드 화면에서 제목 행과 각 열을 선생님이 직접 지정할 수 있도록 확장함.
- 계열별 매트릭스 파일의 과목 열과 셀의 대학 목록을 학생 추천용 과목 레코드로 변환하도록 확장함.
- 학생 추천은 정확 일치 → 같은 학과 → 계열 참고 순으로 조회하고, 학교 선택과목 자료에 없는 추천과목은 확인 필요로 표시함.

## 진행 중

- 결과 검증 및 기준자료 관리 화면의 사용성 개선을 구현하고 회귀 테스트를 완료함.
- 백업/복구 안전장치 구현 및 공개 운영 배포 완료.
- 다중 헤더 기준자료 파서 구현 및 운영 배포 완료.
- 계열별 매트릭스 크래시 수정 및 운영 배포 완료.
- 학생 그룹형 검증 화면에 상태 요약 카드, 반 필터 칩, 과목 태그 표시를 적용함.
- 기준자료 관리에 자료 상태 카드와 업로드 단계 안내를 유지·강화함.
- 상태: 구현·배포 완료. 실제 첨부 PDF를 최종 저장하지 않은 상태로 운영 선생님 로그인 화면까지 접근 확인함.
- 상태: 수정 요청 중심 신청안 검토 흐름 구현·운영 배포 완료.
- 상태: 계열별 과목 선택 가이드 구현·운영 배포 완료.
- 상태: 학교 수강신청 책자 기준자료 구현·테스트·GitHub 반영·운영 배포 완료.
- 상태: 신청안 점검 UX 개편 구현·테스트 완료. Git 커밋·push·운영 배포 대기.

## 남은 작업

- 신청안 점검 UX 개편을 커밋·push·운영 배포하고, 실제 선생님 로그인 상태에서 반별 필터·수정 요청 흐름을 점검한다.

- 실제 HWPX 책자의 표별 헤더·추출 결과를 저장 전 미리보기에서 점검한다. HWP 원본만 있으면 한글에서 HWPX로 저장한 사본이 필요하다.

- 운영 화면에서 실제 계열별 파일을 다시 선택해 다중 헤더 자동 인식, 자동 제외 행 사유, 10건 정규화 미리보기와 저장 전 DB 미변경을 확인.
- 운영 화면에서 실제 2028학년도 파일을 다시 선택해 자동 매핑과 미리보기를 확인.
- 실제 선생님 계정으로 백업 생성·복구 미리보기·최종 복구를 운영 DB에서 점검.
- 실제 선생님 계정으로 로그인 후 모바일/데스크톱 화면 최종 시각 확인.
- 실제 2028학년도 기준자료 엑셀을 받아 제목 행 자동 인식과 열 매핑을 확인.
- 선택 사항: 업로드 이력에서 원본 엑셀 자체를 다시 내려받아야 한다면 R2 파일 보관을 별도 검토.

## 중요 결정사항

- 학생 신청안, 담임 승인 신청안, 공식 수강신청 결과는 별도 데이터다.
- 1차·2차 공식 결과는 별도 보존하며 다음 차수는 직전 공식 결과를 기준으로 시작한다.
- 입학년도와 신청 대상 학년은 서로 다르며, 파일의 2·3학년은 신청 대상 학년이다.
- 학생 식별은 입학년도 + 현재 반 + 현재 번호 + 이름 기준이다.
- 추천과목 미선택은 경고만 표시하고 제출을 차단하지 않는다.
- 담임 확인 완료 뒤 신청안 변경 시 재확인 필요 상태가 된다.
- GitHub `main`이 source of truth이며, 배포 전 반드시 커밋·푸시한다.
- 대학별 추천자료는 상담 참고용이며, 학교 공식 과목 안내자료에 없는 과목을 학생 선택 과목으로 자동 추가하지 않는다.
- 업로드 원본 파일 바이트는 저장하지 않고 파일명·업로드자·일시·건수·적용 상태와 정규화된 기준자료만 저장한다.
- 유사한 과목명은 자동 병합하지 않으며 공백·줄바꿈 정리만 비교에 사용한다.

## 변경된 파일

- `app/planner-app.tsx`: 선생님 메뉴·대시보드·신청안 점검 목록을 예외 학생 점검 및 수정 요청 흐름으로 개편.
- `app/globals.css`: 모바일 신청안 점검 목록의 항목 라벨 표시 보완.

- `lib/school-book-hwpx.ts`: HWPX ZIP/XML의 표·문단 구조를 읽어 학교 과목·선택 규칙을 정규화하는 분석기.
- `app/api/school-book/route.ts`: 학교 책자 미리보기·확정 저장·기존 연도 교체 확인·과목별/일괄 우선 기준 수정 API.
- `app/api/overview/route.ts`, `app/planner-app.tsx`: 적용 중인 학교 책자 데이터를 학생/담임 가이드와 선생님 편집 화면으로 제공.
- `db/schema.ts`, `drizzle/0010_school_course_book_entries.sql`: 학교 수강신청 책자 기준자료 테이블과 조회 인덱스.

- `app/planner-app.tsx`: 계열별 매트릭스 안전 변환 및 기준자료 업로드 오류 경계.
- `lib/reference-sheet.mjs`: 매트릭스 행에서 과목 영역·세부 과목·대학 목록을 추출하는 함수.
- `tests/domain.test.mjs`: 빈 셀·대시 제외와 영역/과목 분리 회귀 테스트.

- `app/planner-app.tsx`: 계열별 전용 헤더 별칭, 다중 헤더 자동 선택, 저장 전 검증·정규화 미리보기·명확한 지표 표시.
- `app/api/reference-import/route.ts`: 계열별 행 분류, 관계 수·샘플 반환, 괄호를 보존한 대학 목록 분리.
- `lib/reference-sheet.mjs`: 헤더 매핑 우선순위와 과목 열 추출 보완.

- `lib/reference-sheet.mjs`: 병합셀 확장, 다중 헤더 결합·자동 감지·열 매핑 유틸리티.
- `tests/domain.test.mjs`: 권역별 대학 자료의 병합 다중 헤더 구조 회귀 테스트.
- `app/planner-app.tsx`: 기준자료 업로드의 헤더 범위 선택·열 번호 매핑·정규화 미리보기.

- `app/api/backup/route.ts`: 인증 비밀값을 제외한 전체 JSON 백업·미리보기·최종 복구 API.
- `app/planner-app.tsx`: 선생님 설정의 전체 백업/백업 복구 UI.

- `app/api/reference-import/route.ts`: 기준자료 분석·미리보기·최종 저장·적용 해제 API.
- `app/api/overview/route.ts`: 적용 중인 기준자료만 학생 추천·선택 화면에 반환.
- `app/planner-app.tsx`: 제목 행·열 매핑, 매트릭스 변환, 검색·필터, 미리보기·이력 UI.
- `db/schema.ts`, `drizzle/0006_crazy_squirrel_girl.sql`: 대학 원문 행·계열 대표 모집단위 구조와 업로드 실패 건수.
- `app/api/education-guide/route.ts`, `lib/education-guide-pdf.ts`, `drizzle/0007_education_office_course_guides.sql`: 교육청 PDF 분석·저장·원문 보존 구조.

## DB 변경

- 학교 수강신청 책자 기준자료를 위한 `drizzle/0010_school_course_book_entries.sql` 추가. 기존 신청안·공식 결과·교육청 PDF·대학 추천 자료와 분리한다.

- `students.current_grade` 추가 마이그레이션 `drizzle/0003_motionless_sentinels.sql`이 적용 대상이다.
- `reference_uploads`, `school_course_guides` 테이블 및 `curricula`/대학 추천 자료 테이블의 `source_upload_id` 추가 마이그레이션 `drizzle/0004_sloppy_onslaught.sql` 생성 완료.
- `university_reference_entries`, `track_reference_entries` 및 `reference_uploads.failed_count` 추가 마이그레이션 `drizzle/0006_crazy_squirrel_girl.sql` 생성 완료.
- 교육청 PDF 과목 안내를 위한 `education_office_course_guides` 테이블 마이그레이션 `drizzle/0007_education_office_course_guides.sql` 추가.
- 마지막 제출 신청안을 보존하는 `plan_submission_snapshots` 테이블 마이그레이션 `drizzle/0008_submission_snapshot_flow.sql` 추가.
- 학생의 선택 계열 열과 학교 계열별 과목 선택 기준 테이블을 위한 `drizzle/0009_academic_track_guides.sql` 추가.

## 테스트 상태

- 신청안 점검 UX 개편 뒤 `npm test` 8건, `npm run build`, `npm run lint`, `git diff --check` 통과.

- 학교 수강신청 책자 기능 뒤 `npm test` 8건, `npm run build`, `npm run lint`, `git diff --check` 통과.

- `npm test`: 8건 통과.
- `npm run build`: 성공.
- `npm run lint`: 성공(줄바꿈 형식 경고만 있음).
- `git diff --check`: 통과.
- 보호된 운영 선생님 화면은 ChatGPT 로그인 필요로 자동 시각 점검 보류.
- 교육청 PDF 기능 보완 뒤 `npm test` 8건, `npm run build`, `npm run lint`, `git diff --check` 통과.
- 운영 Sites 버전 24 배포 성공: `c7211bd3e3273f3255d34192614128e27a125d1b`.
- 신청안 검토 단순화 뒤 `npm test` 8건, `npm run build`, `npm run lint`, `git diff --check` 통과.
- 운영 Sites 버전 25 배포 성공: `9cd39037bd52d38f9ee072e49248aea61f119f35`.
- 계열별 과목 선택 가이드 뒤 `npm test` 8건, `npm run build`, `npm run lint`, `git diff --check` 통과.
- 운영 Sites 버전 26 배포 성공: `231e87a19762a9879b4f633ecdcdbad100b751c0`.

## Git 상태

- 신청안 점검 UX 개편은 커밋·push·배포 전이다.

- 학교 수강신청 책자 기능 커밋 `2d18687`을 GitHub `main`에 push하고 Sites 버전 27으로 운영 배포 성공.

- 마지막 기능 커밋: `6fb6430 feat: expand reference material import workflow`
- 계열별 크래시 수정 커밋 `aa76960`을 GitHub `main`에 push하고 Sites 버전 22로 운영 배포 성공.
- 다중 헤더·정규화 미리보기 보완 커밋 `5fa91a0`을 GitHub `main`에 push하고 Sites 버전 23으로 운영 배포 성공.
- 교육청 PDF 과목 안내 기능 커밋 `e54cc62`, 실제 PDF.js 텍스트 구조 보완 커밋 `c7211bd`를 GitHub `main`에 push하고 Sites 버전 24으로 운영 배포 성공.
- 수정 요청 중심 신청안 검토 커밋 `9cd3903`을 GitHub `main`에 push하고 Sites 버전 25로 운영 배포 성공.
- 계열별 과목 선택 가이드 커밋 `231e87a`를 GitHub `main`에 push하고 Sites 버전 26으로 운영 배포 성공.

## 다음 작업 시작점

신청안 점검 UX 개편을 GitHub `main`에 반영하고 운영 배포한다. 이어 실제 선생님 로그인 상태에서 반별 필터와 수정 요청·재제출 흐름을 확인한다.
